apiVersion: v1
kind: Service
metadata:
  name: {{ include "top.fullname" . }}
  labels:
    {{- include "top.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "top.selectorLabels" . | nindent 4 }}
